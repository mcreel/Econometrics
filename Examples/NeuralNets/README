This directory holds code to train and fit a Temporal Convolutional Net (TCN) to
data generated by the simple DSGE example model.

The directories are
Generate: code to draw the data and put it into a form to be supplied to a net
Net: code to configure the TCN
Train: code to train the net
Fit: code to obtain parameter estimates from data, using the trained net

Usage instructions:

